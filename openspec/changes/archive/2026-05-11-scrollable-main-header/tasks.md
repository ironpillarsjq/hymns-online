## 1. 移除列表布局独立滚动

- [x] 1.1 移除 `.toc-list` 的 `flex: 1` 和 `overflow-y: auto`，改为自动高度

## 2. 移除网格布局独立滚动

- [x] 2.1 移除 `.toc-grid` 的 `flex: 1` 和 `overflow-y: auto`，改为自动高度

## 3. 验证

- [ ] 3.1 验证列表布局下标题随内容一起滚动
- [ ] 3.2 验证网格布局下标题随内容一起滚动
- [ ] 3.3 验证布局切换按钮 `position: fixed` 不受影响
- [ ] 3.4 验证空状态 / 加载 / 错误状态页面正常
