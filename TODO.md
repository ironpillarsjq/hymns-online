- v1
  - [x] 在页面播放界面添加，如果鼠标移到屏幕靠近左侧边得时候，则在屏幕靠近左侧边显示一个小的半透明的预览栏，可滚动预览当前所有的窗口，点击预览栏中的窗口则切换到该窗口的播放界面。
  - [x] 添加一个全局全屏的按钮、点击按钮后，浏览器进入全屏或者退出全屏，按钮呈圆形，位于屏幕顶部中央，当鼠标靠近的时候显示，鼠标离开的时候隐藏。并且：刚进入全屏的时候，全屏按钮显示，随后消失。
  - [x] 底部显示播放进度的组件支持点击输入数字后跳转到对应的页面
  - [x] 浏览页的目录按钮和播放的左右按钮、显示播放进度的组件放在同一个栏中，放在屏幕底部，目录组件和左右按钮都放在右侧底部，左右按钮靠最右侧，进度条的组件还是放在屏幕底部中央，然后显示的逻辑是：当鼠标靠近栏的时候显示，鼠标不动一会儿或离开的时候隐藏，并且该栏也是半透明
  - [x] 在播放界面的预览栏中，显示当前播放页被选中的效果
  - [x] 修改主界面的布局
    - 帮我设置主界面的布局
    - 主界面分为两种布局：列表布局和网格布局
      - 如果文件数在30以下，使用列表布局；如果文件数在30以上，使用网格布局
      - 屏幕的右侧显示一个切换布局的按钮，点击按钮可以在列表布局和网格布局之间切换
      - 列表布局：每行显示一个 PDF 文件
        - 所有 PDF 文件纵向排列，每个文件占一行，行与行之间有适当的间距
        - 名称超过20个字的 PDF 文件名称显示前20个字加省略号，如果名称长度小于20个字，则显示完整名称
        - 列表不能超过屏幕高度，如果 PDF 文件数量过多，列表可以滚动浏览
      
      - 网格布局
        - 每10个 PDF 文件为一小组，放在一个浅色边框的"框"（卡片）内，10个小组为一大组，放在一个浅色边框的"框"（卡片）内
        - 小组
          - 小组内每个单元为一个 PDF 文件，单元水平排列，大屏幕每行显示10个，小屏幕每行显示5个，单元之间有适当的间距
          - 每个单元显示 PDF 文件的展示名称，点击单元可以打开对应的 PDF 文件，名称长度小于等于4，超过4个字的名称显示前4个字加省略号，如果名称长度小于4个字，则显示完整名称
          - 单个单元小一点，因为目标单元数在300-1200。
        - 大组
          - 每个大组中纵向排列10个小组，大屏幕每行显示3个大组，中屏幕每行显示2个大组，小屏幕每行显示1个大组，大组之间有适当的间距
          - 如果组的数量和布局不匹配（如只有1个大组但布局为3列），则该组居中显示
        - 如果所有大组排列不下一个屏幕，组屏幕可以滚动浏览
- v2
  - [x] 接下来我要大修项目
    - 主要修改：将原本的支持pdf浏览转换成图片浏览，文档将不再支持pdf浏览，而是支持图片浏览，图片文件名都是数字，按大小顺序展示
    - 需要修改的功能：
      - 主界面：原来pdf文件的位置，现在变成存放文件夹，然后展示文件夹中的图片
      - 播放界面：把原本支持浏览pdf的功能转换成支持浏览图片的功能，图片文件名都是数字，按大小顺序展示
      - 全屏功能：功能不变
      - 预览栏：功能不变，但是展示的内容变成图片的缩略图，点击缩略图可以切换到对应的图片
      - 控制按钮，功能不变
  - [x] 现在的左右控制按钮有点问题，具体是这样的
    - 当我点击左右切换的时候，在中间的时候没有问题，但是，到了图片到了最后一张的时候，如果点击下一张，那么会提示再次点击退出，这时我点击下一张确实会退出，但是我点击上一张也会退出，这个逻辑不对，如果我点击上一张应该是切换上一张，而不是退出。
    - 同样的道理，当我在第一张图片的时候，如果点击上一张，那么会提示再次点击退出，这时我点击上一张确实会退出，但是我点击下一张也会退出，这个逻辑也不对，如果我点击下一张应该是切换下一张，而不是退出。
  - [x] 浏览界面的图片应该全屏，图片的大小应该适应屏幕，保持图片的宽高比不变
  - [ ] 主界面布局修改
    - 我之前的布局是10个元素为1小组，10个小组为1大组，现在这个组成不变
    - 换行的规则和排列的规则修改一下
      - 小组内元素每5个换行，元素水平排列
      - 小组之间给出适当行间距
      - 大组内，小组之间按列排列
      - 大组之间换行根据屏幕大小，从3 - 1列不等，元素垂直排列
      - 每个小组的第一个元素的字体加粗，以区分小组之间的边界
  - [x] 帮我修改大组内的布局，大组的小组之间，只能纵向排列，现在是小组之间可以横向排列的，这样视觉上会比较乱，改成只能纵向排列，就是小组之间只能呈一列，但是小组内是横向排列，并且这个修改同步到openspec/changes/restructure-toc-grid-layout/tasks.md
  - [x] 主界面，让标题和内容可以一起上下滚动
    - 现在的主界面，标题是固定的，内容是可以滚动的，这样在内容比较多的时候，标题就看不到了，我希望标题和内容可以一起上下滚动，这样在内容比较多的时候，标题也能看到。
- v3
  - [x] 我现在想要扩展项目的功能
    - 比如我现在/public/data下面只有一个文件夹，但是我现在想要支持多个文件夹，然后每个文件夹的功能还是和现在一样，只是有多个页面了
    - 然后data下面的每个文件夹的标题还是页面的标题，比如有文件夹712、853、附录。
    - 现在我需要你帮我更具每个文件夹都生成一个页面，每个页面的功能和现在一样，就是浏览图片的功能，图片的来源是对应文件夹下面的图片，页面的标题是对应文件夹的名称。
    - 但是在所有目录页面的顶部添加一个导航栏，导航栏包含所有文件夹的名称，和一个主页，点击导航栏中的名称可以跳转到对应的页面。
    - 设计一个主页，里面的就两个字：就两个字，下面一句话。然后这句话在外面的配置文件里。
    - 修改：
      - 主页
        - 主页内容直接使用/public/home.svg文件中的内容，然后标题和文字都改成在外部配置文件中配置。
      - 导航栏
        - 导航栏的颜色换成浅灰色
        - 在内容浏览页面不需要导航栏，只需要内容全屏
        - 在内容浏览页面的“目录”按钮点击后，返回目录页，并且从哪一页进入的就返回那一页，比如从712进入就返回712，从853进入就返回853，从附录进入就返回附录，播放结束退出也同样返回对应的目录页
      - 将这些修改同步到tasks.md中
  - [ ] 修改内容浏览页面的“目录”按钮名字为“返回”按钮
  - [ ] 现在主界面网格显示是正常的，但是列表布局怎么不显示了